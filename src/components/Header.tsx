import { Component } from 'solid-js';

const Header: Component = () => {
	// TODO: add login capabilities through a Dialogue from the header
	return (
		<header>
			<h1>Silvam-Operum</h1>
		</header>
	);
}

export default Header;